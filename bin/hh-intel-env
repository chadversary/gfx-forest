#!/usr/bin/env zsh

source "${${(%):-%x}:A:h}/hh-common.zsh" || exit 1

export VK_DRIVER_FILES="$(hh_profile_prefix_dir)/share/vulkan/icd.d/intel_icd.x86_64.json${VK_DRIVER_FILES:+:$VK_DRIVER_FILES}"
exec env "$@"
